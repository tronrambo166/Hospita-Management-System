# Hospita-Management-System (A php script),
import the .sql file to your phpmyadmin,
start apache and mysql from xxamp,
download and put the code files insite htdots folder,
hit "localhost/{foler_path}"
